const id = "m8/web.md";
						const collection = "docs";
						const slug = "m8/web";
						const body = "\n\n# Guía Completa de Instalación y Configuración de Apache en Debian/Ubuntu\n\n## Introducción\nApache es uno de los servidores web más utilizados en el mundo. Este documento te guiará a través de la instalación, configuración y optimización de Apache en sistemas Debian/Ubuntu. Además, incluirá pasos para generar certificados SSL usando OpenSSL, con explicaciones detalladas de cada comando y configuración.\n\n---\n\n## Instalación de Apache\n\n### 1. Actualizar el Sistema\nActualizar los paquetes existentes antes de realizar cualquier instalación:\n```bash\nsudo apt update && sudo apt upgrade -y\n```\n\n### 2. Instalar Apache\nInstalar el servidor Apache:\n```bash\nsudo apt install apache2 -y\n```\n\n### 3. Verificar el Estado del Servicio\nComprobar si Apache está funcionando:\n```bash\nsudo systemctl status apache2\n```\n- **Activo**: Indica que Apache está funcionando.\n- **Inactivo**: Puedes iniciarlo con:\n  ```bash\n  sudo systemctl start apache2\n  ```\n\n### 4. Configurar Inicio Automático\nPara que Apache se inicie al arrancar el sistema:\n```bash\nsudo systemctl enable apache2\n```\n\n---\n\n## Configuración Básica de Apache\n\n### Verificar la Instalación\nAccede a la dirección IP del servidor en tu navegador:\n```\nhttp://<tu-direccion-ip>\n```\nSi la instalación fue exitosa, deberías ver la página predeterminada de Apache.\n\n### Configuración de un Host Virtual\n#### Crear un Archivo para el Host Virtual\n1. Crear el archivo de configuración:\n   ```bash\n   sudo nano /etc/apache2/sites-available/mi-sitio.conf\n   ```\n2. Contenido de ejemplo para el archivo:\n   ```apache\n   <VirtualHost *:80>\n       ServerAdmin admin@misitio.com\n       ServerName misitio.com\n       ServerAlias www.misitio.com\n       DocumentRoot /var/www/misitio\n\n       <Directory /var/www/misitio>\n           Options Indexes FollowSymLinks\n           AllowOverride All\n           Require all granted\n       </Directory>\n\n       ErrorLog ${APACHE_LOG_DIR}/misitio-error.log\n       CustomLog ${APACHE_LOG_DIR}/misitio-access.log combined\n   </VirtualHost>\n   ```\n\n#### Crear el Directorio para el Sitio\nCrear el directorio para alojar los archivos del sitio:\n```bash\nsudo mkdir -p /var/www/misitio\n```\n\n#### Configurar Permisos\nEstablecer permisos adecuados:\n```bash\nsudo chown -R www-data:www-data /var/www/misitio\nsudo chmod -R 755 /var/www/misitio\n```\n\n#### Habilitar la Configuración del Sitio\n1. Activar el archivo del host virtual:\n   ```bash\n   sudo a2ensite mi-sitio.conf\n   ```\n2. Desactivar el sitio por defecto (opcional):\n   ```bash\n   sudo a2dissite 000-default.conf\n   ```\n3. Probar la configuración:\n   ```bash\n   sudo apache2ctl configtest\n   ```\n4. Reiniciar Apache:\n   ```bash\n   sudo systemctl reload apache2\n   ```\n\n---\n\n## Habilitar Módulos en Apache\n\n| Módulo         | Descripción                                             | Comando para Activar           |\n|-----------------|---------------------------------------------------------|--------------------------------|\n| mod_rewrite     | Habilitar reescritura de URLs                           | `sudo a2enmod rewrite`         |\n| mod_ssl         | Soporte para conexiones SSL                            | `sudo a2enmod ssl`             |\n| mod_headers     | Control de cabeceras HTTP                              | `sudo a2enmod headers`         |\n\nReiniciar Apache después de habilitar cualquier módulo:\n```bash\nsudo systemctl reload apache2\n```\n\n---\n\n## Configuración SSL con OpenSSL\n\nGenerar certificados SSL para conexiones seguras:\n\n### 1. Instalar OpenSSL\n```bash\nsudo apt install openssl -y\n```\n\n### 2. Crear un Certificado Auto-firmado\nEjecutar los siguientes comandos:\n1. Crear un directorio para almacenar los certificados:\n   ```bash\n   sudo mkdir /etc/apache2/ssl\n   ```\n2. Generar una clave privada y certificado auto-firmado:\n   ```bash\n   sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \\\n   -keyout /etc/apache2/ssl/misitio.key -out /etc/apache2/ssl/misitio.crt\n   ```\n   **Parámetros Importantes:**\n   - `-x509`: Crear un certificado X.509.\n   - `-nodes`: No cifrar la clave privada.\n   - `-days 365`: Validez del certificado (en días).\n   - `-newkey rsa:2048`: Crear una nueva clave RSA de 2048 bits.\n   \n   Durante la generación se te pedirá rellenar la siguiente información:\n   | Campo         | Descripción                          |\n   |---------------|--------------------------------------|\n   | Country Name  | Código del país (e.g., `ES`)       |\n   | State/Region  | Provincia o estado                  |\n   | Organization  | Nombre de tu organización          |\n   | CN (Common Name)| Nombre del dominio                |\n\n### 3. Configurar Apache para Usar SSL\n1. Editar la configuración del host virtual:\n   ```bash\n   sudo nano /etc/apache2/sites-available/mi-sitio-ssl.conf\n   ```\n2. Contenido del archivo:\n   ```apache\n   <VirtualHost *:443>\n       ServerAdmin admin@misitio.com\n       ServerName misitio.com\n       DocumentRoot /var/www/misitio\n\n       SSLEngine on\n       SSLCertificateFile /etc/apache2/ssl/misitio.crt\n       SSLCertificateKeyFile /etc/apache2/ssl/misitio.key\n\n       <Directory /var/www/misitio>\n           Options Indexes FollowSymLinks\n           AllowOverride All\n           Require all granted\n       </Directory>\n\n       ErrorLog ${APACHE_LOG_DIR}/misitio-error.log\n       CustomLog ${APACHE_LOG_DIR}/misitio-access.log combined\n   </VirtualHost>\n   ```\n3. Activar configuración SSL:\n   ```bash\n   sudo a2ensite mi-sitio-ssl.conf\n   ```\n4. Habilitar módulo SSL (si no está habilitado):\n   ```bash\n   sudo a2enmod ssl\n   ```\n5. Reiniciar Apache:\n   ```bash\n   sudo systemctl reload apache2\n   ```\n\n---\n\n## Monitorización y Logs\n\n| Archivo              | Descripción                              |\n|----------------------|------------------------------------------|\n| `/var/log/apache2/access.log` | Registro de accesos al servidor    |\n| `/var/log/apache2/error.log`  | Registro de errores del servidor   |\n\nMonitorizar los logs en tiempo real:\n```bash\nsudo tail -f /var/log/apache2/access.log /var/log/apache2/error.log\n```\n\n---\n\n## Desinstalación de Apache\n\n### Detener y Deshabilitar Apache\n```bash\nsudo systemctl stop apache2\nsudo systemctl disable apache2\n```\n\n### Eliminar Apache\n```bash\nsudo apt purge apache2 -y\nsudo apt autoremove -y\n```\n\n### Eliminar Directorios Residuos\n```bash\nsudo rm -rf /etc/apache2 /var/www/html\n```\n";
						const data = {title:"WEB Linux",description:"WEB Linux",editUrl:true,head:[],template:"doc",sidebar:{hidden:false,attrs:{}},pagefind:true,draft:false};
						const _internal = {
							type: 'content',
							filePath: "/root/M7_doc/src/content/docs/m8/web.md",
							rawData: undefined,
						};

export { _internal, body, collection, data, id, slug };
